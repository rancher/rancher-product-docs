name: Tasks needing write permissions

on:
  workflow_run:
    workflows: ["Test deployment"]
    types: [completed]
jobs:
  write-tasks:
    uses: rancher/product-docs-common/.github/workflows/write-tasks.yml@main
