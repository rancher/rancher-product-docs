name: Tasks needing write permissions

on:
  workflow_run:
    workflows: ["Test Product deployment"]
    types: [completed]
jobs:
  write-tasks:
    uses: rancher/product-docs-common/.github/workflows/write-tasks.yml@776dee012d9ab2f661cdbdeeb16e6ab81e965d5a # main
