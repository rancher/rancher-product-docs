= Configure Microsoft Entra ID
:page-languages: [en, de, es, fr, ja, pt, zh]
:revdate: 2025-11-28
:page-revdate: {revdate}
:community-path: how-to-guides/new-user-guides/authentication-permissions-and-global-configuration/authentication-config/configure-azure-ad.adoc 
:product-path: rancher-admin/users/authn-and-authz/configure-azure-ad.adoc
ifeval::["{build-type}" != "community"]
:xref-authn-authz: xref:rancher-admin/users/authn-and-authz/authn-and-authz.adoc
:xref-backup-cluster: xref:rancher-admin/back-up-restore-and-disaster-recovery/back-up.adoc
:xref-back-restore-example-configs: xref:rancher-admin/back-up-restore-and-disaster-recovery/configuration/examples.adoc
:xref-rancher-restore: xref:rancher-admin/back-up-restore-and-disaster-recovery/restore.adoc
endif::[]
ifeval::["{build-type}" == "community"]
:xref-authn-authz: xref:how-to-guides/new-user-guides/authentication-permissions-and-global-configuration/authentication-config/index.adoc
:xref-backup-cluster: xref:how-to-guides/new-user-guides/backup-restore-and-disaster-recovery/back-up-rancher.adoc
:xref-back-restore-example-configs: xref:reference-guides/backup-restore-configuration/examples.adoc
:xref-rancher-restore: xref:how-to-guides/new-user-guides/backup-restore-and-disaster-recovery/restore-rancher.adoc
endif::[]

[#_microsoft_graph_api]
== Microsoft Entra ID (Microsoft Graph API)

Microsoft Graph API is the required flow for configuring Microsoft Entra ID (formerly Azure AD) authentication in Rancher. The sections below assist <<_new_user_setup,new users>> in configuring Microsoft Entra ID with a new instance and guide existing app owners in <<_migrating_from_azure_ad_graph_api_to_microsoft_graph_api,migrating to the Microsoft Graph API flow>>.

[#_new_user_setup]
=== New User Setup

If you have a Microsoft Entra ID tenant, you can configure Rancher to allow users to log in using their directory credentials. Configuring Microsoft Entra ID authentication requires setup steps in both the Microsoft Azure portal and Rancher.

[NOTE]
.Notes
====
* Microsoft Entra ID integration only supports Service Provider–initiated logins.
* Most of this procedure takes place in the https://portal.azure.com/[Microsoft Azure Portal].
====

[#_azure_active_directory_configuration_outline]
==== Microsoft Entra ID Configuration Outline

Configuring Rancher to allow users to authenticate with their Microsoft Entra ID accounts involves multiple procedures. Review the outline below before getting started.

[TIP]
====
Before you start, open two browser tabs: one for Rancher, and one for the Azure portal. This will make it easier to copy and paste configuration values between the two interfaces.
====

[#_1_register_rancher_with_azure]
==== 1. Register Rancher with Azure

Before enabling Microsoft Entra ID within Rancher, you must register Rancher as an application in your tenant.

. Log in to the https://portal.azure.com/[Microsoft Azure Portal] as an administrative user (Global Administrator or Application Administrator).
. Search for and select *Microsoft Entra ID* (formerly _Azure Active Directory_).
. In the left navigation menu, select *App registrations*.
. Click *+ New registration* and complete the form.
+
image::new-app-registration.png[New App Registration]

 .. Enter a *Name* (such as `Rancher`).
 .. Under *Supported account types*, select *Accounts in this organizational directory only (Single tenant)*.
 .. In the *Redirect URI* section, ensure *Web* is selected in the platform dropdown menu. Enter your Rancher Server URL appended with the verification path: `<MY_RANCHER_URL>/verify-auth-azure`.   
+
[TIP]
====
You can find your exact Redirect URI in Rancher by navigating to the **Azure AD** authentication setup page (*Global View > Users & Authentication > Auth Provider > AzureAD*).
====

 .. Click *Register*.

[NOTE]
====
It can take up to five minutes for changes in Microsoft Entra ID to take effect globally.
====

[#_2_create_a_new_client_secret]
==== 2. Create a new client secret

Create a client secret that Rancher will use to authenticate with Microsoft Entra ID.

. From *Microsoft Entra ID > App registrations*, select your newly created Rancher application.
+
image::open-rancher-app-reg.png[Open Rancher Registration]

. In the left navigation menu under *Manage*, click *Certificates & secrets*.
. Select the *Client secrets* tab, then click *+ New client secret*.
+
image::new-client-secret.png[Create new client secret]

. Enter a *Description* (such as `Rancher`).
. Select an expiration window under *Expires*.
+
NOTE: Shorter durations are more secure but require rotating the secret more frequently. If the application secret expires, users will not be able to log in to Rancher until the secret is rotated in Azure and updated in Rancher.

. Click *Add*.
. Copy the string in the *Value* column immediately.
+
[IMPORTANT]
====
Copy the secret *Value* now. Once you navigate away from this page or refresh, the secret value will be permanently masked (`••••••••••••••••`). Do not copy the *Secret ID*.
====

[#_3_set_required_permissions_for_rancher]
==== 3. Set Required Permissions for Rancher

Next, set the required Microsoft Graph API permissions for the application registration.

[CAUTION]
====
Ensure that you set **Application permissions**, and _not_ **Delegated permissions**. Delegated permissions will cause login failures.
====

. In the left navigation menu under *Manage*, select *API permissions*.
. Click *+ Add a permission*.
. Select **Microsoft Graph**, then select **Application permissions**.
. Search for and add the following permission: `Directory.Read.All`
+
image::api-permissions.png[Select API Permissions]

. Click *Add permissions*.
. Back on the *API permissions* page, click *Grant admin consent for <Tenant Name>*, then click *Yes* to confirm.
+
image::select-req-permissions.png[Open Required Permissions]

[NOTE]
====
Rancher performs user and group lookups via the Graph API and requires permissions to:

* Get a user / List all users
* Get a group / List all groups
* List group memberships for a user

Alternative permission combinations that satisfy these requirements include:

* `Directory.Read.All`
* `User.Read.All` and `GroupMember.Read.All`
* `User.Read.All` and `Group.Read.All`
====

[#_4_allow_public_client_flows]
==== 4. Allow Public Client Flows

To allow authentication from the Rancher CLI, enable public client flows:

. In the left navigation menu under *Manage*, select *Authentication*.
. Scroll to *Advanced settings*.
. Under *Allow public client flows*, select **Yes**.
. Click **Save**.
+
image::azure-public-client-flows.png[Allow Public Client Flows]

[#_5_copy_azure_application_data]
==== 5. Copy Microsoft Entra ID Application Data

image::app-configuration.png[Application ID]

. Obtain your **Tenant ID**:
 .. Go to **Microsoft Entra ID > Overview** (or *App registrations* > your application's *Overview*).
 .. Copy the **Directory (tenant) ID**.
. Obtain your **Application (Client) ID**:
 .. On your app registration's **Overview** page, copy the **Application (client) ID**.
. Standard and China cloud environments only require entering the **Tenant ID**, **Application (Client) ID**, and **Client Secret Value**.

image::tenant-application-id-secret.png[Standard Endpoint Options]

*For Custom Endpoints:*

[CAUTION]
====
Custom Endpoints are not tested or officially supported by Rancher.
====

To locate custom endpoint URLs:

* From your app registration's *Overview* page, click **Endpoints** in the top toolbar.
* Select the v1 or v2 endpoints matching your deployment needs:
 ** **Microsoft Graph API endpoint** (Graph Endpoint)
 ** **OAuth 2.0 token endpoint** (Token Endpoint)
 ** **OAuth 2.0 authorization endpoint** (Auth Endpoint)

[#_6_configure_azure_ad_in_rancher]
==== 6. Configure Microsoft Entra ID in Rancher

Complete the setup in the Rancher UI:

. Log into Rancher.
. In the top left menu, select **☰ > Users & Authentication**.
. In the left menu, click **Auth Provider**.
. Click **AzureAD**.
. Complete the form using the values obtained in <<_5_copy_azure_application_data,Step 5>>.
+
[CAUTION]
====
The account completing this setup will be granted Rancher administrator privileges. Ensure that this level of access is appropriate before proceeding.
====
+
*Standard / China Cloud Mapping:*
+
|===
| Rancher Field | Microsoft Entra ID Value

| Tenant ID
| Directory (tenant) ID

| Application ID
| Application (client) ID

| Application Secret
| Client Secret Value

| Endpoint
| `https://login.microsoftonline.com/`
|===
+
*Custom Endpoint Mapping:*
+
|===
| Rancher Field | Microsoft Entra ID Value

| Graph Endpoint
| Microsoft Graph API Endpoint

| Token Endpoint
| OAuth 2.0 Token Endpoint

| Auth Endpoint
| OAuth 2.0 Authorization Endpoint
|===
+
[IMPORTANT]
====
When entering a custom Graph Endpoint URL, strip the tenant ID suffix from the path (e.g., use `https://graph.microsoft.com` instead of `https://graph.microsoft.com/<tenant-id>`).
====

. (Optional) Filter group memberships to reduce log volume and token overhead (Rancher v2.9.0+). See <<_filtering_users_by_azure_ad_auth_group_memberships,Filtering Users by Azure AD Auth Group Memberships>> for details.
. Click **Enable**.

*Result:* Microsoft Entra ID authentication is now active.

[#__optional_configure_authentication_with_multiple_rancher_domains]
==== (Optional) Configure Authentication with Multiple Rancher Domains

When managing multiple Rancher domains, additional Redirect URIs must be configured manually in the underlying `azuread` authentication resource in Rancher. By default, authentication requests redirect to the primary Redirect URI registered in <<_1_register_rancher_with_azure,Step 1>>.

[#_migrating_from_azure_ad_graph_api_to_microsoft_graph_api]
=== Migrating from Azure AD Graph API to Microsoft Graph API

The legacy **Azure AD Graph API** has been retired by Microsoft. Rancher setups using the legacy endpoints must update to **Microsoft Graph API** to prevent authentication failures.

[#_updating_endpoints_in_the_rancher_ui]
==== Updating Endpoints in the Rancher UI

[CAUTION]
====
Create a full {xref-backup-cluster}[Rancher backup] prior to initiating the endpoint migration.
====

. Ensure your app registration in Azure has been updated with the required <<_3_set_required_permissions_for_rancher,Microsoft Graph permissions>>.
. Log into Rancher as an administrator.
. Click the notification banner prompting an update for Azure AD / Entra ID authentication.
+
image::rancher-ui-azure-update.png[Rancher UI Banner]

. Click **Update Endpoint**.
+
image::rancher-button-to-update.png[Update Endpoint]

. Confirm the action in the pop-up dialog.
+
image::azure-update-popup.png[Azure Update Pop-up]

[#_air_gapped_environments]
==== Air-Gapped Environments

Air-gapped environments must ensure network access and outbound firewall rules allow access to the new Microsoft Graph endpoints (`https://graph.microsoft.com`).

[#_rolling_back_the_migration]
==== Rolling Back the Migration

If recovery or secret rotation is required post-migration:

. Restore from backup using the standard {xref-rancher-restore}[Rancher restore procedure].
. To rotate the Application Secret in Rancher, update the Kubernetes secret `azureadconfig-applicationsecret` in the `cattle-global-data` namespace.

==== Endpoint Reference (Global)

|===
| Field | Legacy Endpoint (Deprecated) | New Endpoint (Microsoft Graph)

| Auth Endpoint
| `\https://login.microsoftonline.com/{tenantID}/oauth2/authorize`
| `\https://login.microsoftonline.com/{tenantID}/oauth2/v2.0/authorize`

| Endpoint
| `\https://login.microsoftonline.com/`
| `\https://login.microsoftonline.com/`

| Graph Endpoint
| `\https://graph.windows.net/`
| `\https://graph.microsoft.com`

| Token Endpoint
| `\https://login.microsoftonline.com/{tenantID}/oauth2/token`
| `\https://login.microsoftonline.com/{tenantID}/oauth2/v2.0/token`
|===

==== Endpoint Reference (China)

|===
| Field | Legacy Endpoint (Deprecated) | New Endpoint (Microsoft Graph)

| Auth Endpoint
| `\https://login.chinacloudapi.cn/{tenantID}/oauth2/authorize`
| `\https://login.partner.microsoftonline.cn/{tenantID}/oauth2/v2.0/authorize`

| Endpoint
| `\https://login.chinacloudapi.cn/`
| `\https://login.partner.microsoftonline.cn/`

| Graph Endpoint
| `\https://graph.chinacloudapi.cn/`
| `\https://microsoftgraph.chinacloudapi.cn`

| Token Endpoint
| `\https://login.chinacloudapi.cn/{tenantID}/oauth2/token`
| `\https://login.partner.microsoftonline.cn/{tenantID}/oauth2/v2.0/token`
|===

[#_filtering_users_by_azure_ad_auth_group_memberships]
== Filtering Users by Microsoft Entra ID Group Memberships

In Rancher v2.9.0 and later, you can filter group memberships retrieved from Microsoft Entra ID using OData filter expressions.

[CAUTION]
====
Filtering out a group membership prevents Rancher from seeing that the user belongs to that group. As a result, users will lose any roles or access rights granted via excluded groups.
====

. In Rancher, navigate to **☰ > Users & Authentication > Auth Provider > AzureAD**.
. Check **Limit users by group membership**.
. Enter an https://learn.microsoft.com/en-us/odata/concepts/queryoptions-overview#filter[OData filter expression] into the **Group Membership Filter** field (e.g., `startswith(displayName,'test')`).

image::auth-setup-azure-ad-filter.png[Adding a group membership filter to Azure AD]

[#_deprecated_azure_ad_graph_api]
== Deprecated Azure AD Graph API

[IMPORTANT]
====
* The Azure AD Graph API is retired by Microsoft. All Rancher installations authenticating with Microsoft Entra ID must utilize the Microsoft Graph API flow.
====

[#_azure_ad_roles_claims]
== Microsoft Entra ID Roles Claims

Rancher supports the `roles` claim provided in the Microsoft Entra ID OIDC token, enabling centralized Role-Based Access Control (RBAC) mapping.

By mapping `roles` claims, administrators can:

* Define specific application roles in Microsoft Entra ID.
* Bind Entra ID roles directly to `ProjectRole` or `ClusterRole` resources within Rancher.
* Delegate identity and role management completely to the external identity provider.

For example, consider the following role structure in Microsoft Entra ID:

|===
| Microsoft Entra ID Role Name | Members

| project-alpha-dev
| User A, User C
|===

When User A logs into Rancher using Microsoft Entra ID, their OIDC token includes a `roles` claim containing `["project-alpha-dev"]`. Rancher processes the token and adds `project-alpha-dev` to User A's internal list of user groups and roles. Because an administrator created a Project Role Binding that maps the Entra ID role `project-alpha-dev` to the Rancher `Dev Member` role for Project Alpha, User A is automatically granted `Dev Member` permissions.
