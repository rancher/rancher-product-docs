= kubectlユーティリティ
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2025-08-05
:page-revdate: {revdate}
:community-path: reference-guides/cli-with-rancher/kubectl-utility.adoc 
:product-path: rancher-admin/cli/kubectl.adoc
ifeval::["{build-type}" != "community"]
:xref-rancher-cli: xref:rancher-admin/cli/rancher-cli.adoc
endif::[]
ifeval::["{build-type}" == "community"]
:xref-rancher-cli: xref:reference-guides/cli-with-rancher/rancher-cli.adoc
endif::[]

[#_kubectl]
== kubectl

kubectlを使用してRancherと対話します。

[#_kubectl_utility]
=== kubectlユーティリティ

`kubectl`ユーティリティをインストールします。 https://kubernetes.io/docs/tasks/tools/install-kubectl/[kubectlをインストールする]を参照してください。

Rancher Web UIでクラスターにアクセスし、`Kubeconfig`をクリックして内容をコピーし、それを`~/.kube/config`ファイルに貼り付けることでkubectlを構成します。

`kubectl cluster-info`または`kubectl get pods`を正常に実行します。

[#_authentication_with_kubectl_and_kubeconfig_tokens_with_ttl]
=== kubectlおよびTTL付きkubeconfigトークンによる認証

_要件_

管理者がxref:api/api-tokens.adoc#_disable_tokens_in_generated_kubeconfigs[kubeconfigトークン生成を無効にしている場合、kubeconfigファイルは、`kubectl`を実行する際に、{xref-rancher-cli}[Rancher CLI]がパス内に存在している必要があります。そうでない場合、次のようなエラーが表示されます：
`Unable to connect to the server: getting credentials: exec: exec: "rancher": executable file not found in $PATH`。

この機能により、kubectlはRancherサーバーと認証し、必要に応じて新しいkubeconfigトークンを取得できます。現在サポートされている認証プロバイダーは次のとおりです：

. ローカル
. Active Directory（LDAPのみ）
. FreeIPA
. OpenLDAP
. SAMLプロバイダー：Ping、Okta、ADFS、Keycloak、Shibboleth
. Azure AD

最初にkubectlを実行するとき、例えば`kubectl get pods`、認証プロバイダーを選択し、Rancherサーバーにログインするように求められます。kubeconfigトークンは、kubectlを実行するパス内の`./.cache/token`にキャッシュされます。このトークンはxref:api/api-tokens.adoc#_disable_tokens_in_generated_kubeconfigs[期限切れになるまで]、またはxref:api/api-tokens.adoc#_deleting_tokens[Rancherサーバーから削除されるまで]有効です。期限が切れた場合、`kubectl get pods`コマンドを実行するために再度Rancherサーバーにログインする必要があります。
