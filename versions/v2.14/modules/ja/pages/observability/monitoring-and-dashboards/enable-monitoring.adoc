= 監視を有効にする
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2025-09-29
:page-revdate: {revdate}
:community-path: how-to-guides/advanced-user-guides/monitoring-alerting-guides/enable-monitoring.adoc 
:product-path: observability/monitoring-and-dashboards/enable-monitoring.adoc
:experimental:
ifeval::["{build-type}" != "community"]
:xref-cluster-project-roles: xref:rancher-admin/users/authn-and-authz/manage-role-based-access-control-rbac/cluster-and-project-roles.adoc
:xref-global-permissions: xref:rancher-admin/users/authn-and-authz/manage-role-based-access-control-rbac/global-permissions.adoc
:xref-secrets: xref:security/secrets-hub.adoc
:xref-monitoring-helm-chart-options: xref:observability/monitoring-and-dashboards/configuration/helm-chart-options.adoc
:xref-receivers: xref:observability/monitoring-and-dashboards/configuration/receivers.adoc
:xref-how-monitoring-works: xref:observability/monitoring-and-dashboards/how-monitoring-works.adoc
endif::[]
ifeval::["{build-type}" == "community"]
:xref-cluster-project-roles: xref:how-to-guides/new-user-guides/authentication-permissions-and-global-configuration/manage-role-based-access-control-rbac/cluster-and-project-roles.adoc
:xref-global-permissions: xref:how-to-guides/new-user-guides/authentication-permissions-and-global-configuration/manage-role-based-access-control-rbac/global-permissions.adoc 
:xref-secrets: xref:how-to-guides/new-user-guides/kubernetes-resources-setup/secrets.adoc
:xref-monitoring-helm-chart-options: xref:reference-guides/monitoring-v2-configuration/helm-chart-options.adoc
:xref-receivers: xref:reference-guides/monitoring-v2-configuration/receivers.adoc
:xref-how-monitoring-works: xref:integrations-in-rancher/monitoring-and-alerting/how-monitoring-works.adoc
endif::[]

{xref-global-permissions}[管理者]または{xref-cluster-project-roles}#_cluster_roles[クラスターオーナー]として、Rancherを構成してKubernetesクラスターを監視するためにPrometheusをデプロイできます。

このページでは、新しい監視アプリケーションを使用してクラスター内で監視とアラートを有効にする方法について説明します。

SSLの有無にかかわらず、監視を有効にできます。

[#_requirements]
== 要件

* 各ノードのポート9796でトラフィックを許可してください。Prometheusはこれらのポートからメトリクスを取得します。
 ** {xref-how-monitoring-works}#_pushprox[PushProx]が無効になっている場合（`ingressNginx.enabled`が`false`に設定されている場合）や、以前のRancherバージョンからアップグレードした場合は、各ノードのポート10254でもトラフィックを許可する必要があります。
* クラスターがリソース要件を満たしていることを確認してください。クラスターには、少なくとも1950Miのメモリ、2700mのCPU、および50Giのストレージが必要です。リソースの制限と要求の内訳については、{xref-monitoring-helm-chart-options}#_configuring_resource_limits_and_requests[リソースの制限と要求の設定]を参照してください。

[NOTE]
====

Alertmanager、Grafana、またはIngressを設定する場合は、Helmチャートデプロイメントの設定で行う必要があります。デプロイメントの外でIngressを作成することは問題があります。
====


[#_setting_resource_limits_and_requests]
== リソース制限とリクエストの設定

リソースのリクエストと制限は、`rancher-monitoring`をインストールする際に設定できます。 Rancher UIからPrometheusリソースを構成するには、左上隅のmenu:アプリ[監視]をクリックしてください。

デフォルトの制限に関する詳細については、{xref-monitoring-helm-chart-options}#_configuring_resource_limits_and_requests[このページ]を参照してください。

[#_install_the_monitoring_application]
== 監視アプリケーションをインストールする

[#_enable_monitoring_for_use_without_ssl]
=== SSLなしで使用するための監視を有効にする

. *☰ > クラスター管理*をクリックします。
. 作成したクラスターに移動し、*探索*をクリックします。
. *クラスターツール*をクリックします（左下隅）。
. 監視の横にある*インストール*をクリックします。
. オプション:Valuesステップで、アラート、Prometheus、およびGrafanaのリクエスト、リミットなどをカスタマイズします。ヘルプについては、{xref-monitoring-helm-chart-options}[設定リファレンス]を参照してください。

*結果:*監視アプリは`cattle-monitoring-system`ネームスペースにデプロイされます。

[#_enable_monitoring_for_use_with_ssl]
=== SSLで使用するための監視を有効にします。

. SSLをアラートに使用するために、{xref-secrets}[このページ]の手順に従ってシークレットを作成してください。
 ** シークレットは`cattle-monitoring-system`ネームスペースに作成する必要があります。存在しない場合は、最初に作成してください。
 ** シークレットに`ca`、`cert`、および`key`ファイルを追加します。
. 左上隅で、*☰ > クラスター管理*をクリックします。
. *クラスター*ページで、SSLで使用するために監視を有効にしたいクラスターに移動し、*探索*をクリックします。
. menu:アプリ[チャート]をクリックしてください。
. *監視*をクリックします。
. 監視をすでにインストールしているかどうかに応じて、*インストール*または*更新*をクリックします。
. *インストール前にHelmオプションをカスタマイズ*のチェックボックスをオンにし、*次へ*をクリックします。
. *アラート*をクリックします。
. *追加のシークレット*フィールドに、以前に作成したシークレットを追加します。

*結果:*監視アプリは`cattle-monitoring-system`ネームスペースにデプロイされます。

{xref-receivers}#_rancher_uiで受信者を作成する際に、SSL対応の受信者（メールやWebhookなど）は、*SSL*セクションに*CAファイルパス*、*証明書ファイルパス*、および*キー ファイルパス*のフィールドを持ちます。これらのフィールドに`ca`、`cert`、および`key`の各パスを入力します。パスは`/etc/alertmanager/secrets/name-of-file-in-secret`の形式になります。

たとえば、これらのキーと値のペアを持つシークレットを作成した場合：

[,yaml]
----
ca.crt=`base64-content`
cert.pem=`base64-content`
key.pfx=`base64-content`
----

その場合、*証明書ファイルパス*は`/etc/alertmanager/secrets/cert.pem`に設定されます。

[#_rancher_performance_dashboard]
== Rancherパフォーマンスダッシュボード

アップストリーム（ローカルクラスタ）に監視がインストールされると、CPUやメモリデータなど、Rancherポッドに関する基本的な健康指標が提供されます。ローカルRancherサーバーの高度なメトリクスを取得するには、Grafana用のRancherパフォーマンスダッシュボードを追加で有効にする必要があります。

このダッシュボードでは、以下の高度な指標にアクセスできます：

* ハンドラーの平均実行時間（過去5分間）
* Rancher APIの平均リクエスト時間（過去5分間）
* サブスクライブの平均リクエスト時間（過去5分間）
* ラッソコントローラーの作業キューの深さ（上位20）
* Rancherリクエストの数（上位20）
* 失敗した Rancher API リクエストの数（上位20）
* K8sプロキシストアの平均リクエスト時間（過去5分間）（上位20）
* K8sプロキシクライアントの平均リクエスト時間（過去5分間）（上位20）
* グループバージョン種別によるキャッシュされたオブジェクト（上位20）
* ラッソハンドラーの実行（上位20）
* 過去2分間のハンドラーの実行（上位20）
* エラーを伴うハンドラーの総実行数（上位20）
* リモートダイヤラーセッションによって送信されたデータ（上位20）
* リモートダイヤラーセッションのエラー（上位20）
* 削除されたリモートダイヤラー接続（上位20）
* クライアントによって追加されたリモートダイヤラー接続（上位20）

[NOTE]
====

プロファイリングデータ（高度なメモリやCPU分析など）は、非常にコンテキスト依存の技術であり、デバッグ用であり、通常の観察には意図されていないため、存在しません。
====


[#_enabling_the_rancher_performance_dashboard]
=== Rancherパフォーマンスダッシュボードを有効にする

Rancherパフォーマンスダッシュボードを有効にするには：

[tabs,sync-group-id=UIorCLI]
======
Helm::
+
--
Helm CLI で次のオプションを使用します:

[,bash]
----
--set extraEnv\[0\].name="CATTLE_PROMETHEUS_METRICS" --set-string extraEnv\[0\].value=true
----

Rancher Helm チャートの values.yaml ファイルに次のスニペットを含めることもできます:

[,yaml]
----
extraEnv:
  - name: "CATTLE_PROMETHEUS_METRICS"
    value: "true"
----
--

UI::
+
--
:experimental:

. *☰ > クラスター管理*をクリックします。
. `local` クラスターの行に移動し、*探索* をクリックします。
. menu:ワークロード[デプロイメント] をクリックします。
. 上部のドロップダウンメニューを使用して *すべてのネームスペース* をフィルタリングします。
. `cattle-system` ネームスペースの下で、`rancher` 行に移動し、*⋮ > 設定を編集* をクリックします。
. *環境変数* の下で、*変数を追加* をクリックします。
. *タイプ* には `Key/Value Pair` を選択します。
. *変数名* には `CATTLE_PROMETHEUS_METRICS` を入力します。
. *値* には `true` を入力します。
. 変更を適用するには *保存* をクリックします。
--
======

[#_accessing_the_rancher_performance_dashboard]
=== Rancher パフォーマンスダッシュボードへのアクセス

. *☰ > クラスター管理*をクリックします。
. `local` クラスターの行に移動し、*探索* をクリックします。
. *監視* をクリックします。
. *Grafana* ダッシュボードを選択します。
. サイドバーから *ダッシュボードを検索* をクリックします。
. `Rancher Performance Debugging` を入力し、それを選択します。
