= Usando Tokens de API
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2026-04-06
:page-revdate: {revdate}
:community-path: api/api-tokens.adoc 
:product-path: api/api-tokens.adoc
ifeval::["{build-type}" != "community"]
:xref-api-reference: xref:api/reference.adoc
:xref-rancher-cli: xref:rancher-admin/cli/rancher-cli.adoc
:xref-kubeconfig-file: xref:cluster-admin/manage-clusters/access-clusters/authorized-cluster-endpoint.adoc
:xref-kubectl: xref:rancher-admin/cli/kubectl.adoc
:xref-experimental-features: xref:rancher-admin/experimental-features/experimental-features.adoc
endif::[]
ifeval::["{build-type}" == "community"]
:xref-api-reference: xref:api/api-reference.adoc
:xref-rancher-cli: xref:reference-guides/cli-with-rancher/rancher-cli.adoc
:xref-kubeconfig-file: xref:how-to-guides/new-user-guides/manage-clusters/access-clusters/authorized-cluster-endpoint.adoc
:xref-kubectl: xref:reference-guides/cli-with-rancher/kubectl-utility.adoc
:xref-experimental-features: xref:how-to-guides/advanced-user-guides/enable-experimental-features/index.adoc
endif::[]

include::shared:ROOT:partial$en/v3-api-tokens-deprecation-warning.adoc[]

O Rancher v2.8.0 introduziu a {xref-api-reference}[API Kubernetes do Rancher] que pode ser usada para gerenciar recursos do Rancher através de `kubectl`. Esta página cobre informações sobre tokens de API usados com o {xref-rancher-cli}[Rancher CLI], {xref-kubeconfig-file}#_sobre_o_arquivo_kubeconfig[arquivos kubeconfig], Terraform e o xref:api/v3-rancher-api-guide.adoc#_enable_view_in_api[navegador da API v3].

Por padrão, alguns tokens de API em nível de cluster são gerados com tempo de vida infinito (`ttl=0`). Em outras palavras, tokens de API com `ttl=0` nunca expiram, a menos que você os invalide. Tokens não são invalidados ao alterar uma senha.

Você pode desativar tokens de API excluindo-os ou desativando a conta do usuário.

[#_deleting_tokens]
== Excluindo tokens

Para excluir um token:

. Vá para a lista de todos os tokens na visualização da API do Rancher em `\https://<Rancher-Server-IP>/v3/tokens`.
. Acesse o token que você deseja excluir pelo seu ID. Por exemplo, `\https://<Rancher-Server-IP>/v3/tokens/kubectl-shell-user-vqkqt`
. Clique em *Excluir*.

A seguir está uma lista completa de tokens gerados com `ttl=0`:

|===
| Token | Descrição

| `kubectl-shell-*`
| Acesso ao shell `kubectl` no navegador

| `agent-*`
| Token para implantação de agente

| `compose-token-*`
| Token para composição

| `helm-token-*`
| Token para implantação de gráfico Helm

| `drain-node-*`
| Token para drain (o Rancher usa `kubectl` para drain porque não há uma API nativa do Kubernetes).
|===

[#_setting_ttl_on_kubeconfig_tokens]
== Definindo TTL em tokens Kubeconfig

Os administradores podem definir um tempo de vida global (TTL) para os tokens Kubeconfig. Alterar o TTL padrão do kubeconfig pode ser feito navegando até as configurações globais e definindo <<_kubeconfig_default_token_ttl_minutes,`kubeconfig-default-token-ttl-minutes`>> para a duração desejada em minutos. A partir do Rancher v2.8, o valor padrão de <<_kubeconfig_default_token_ttl_minutes,`kubeconfig-default-token-ttl-minutes`>> é `43200`, o que significa que os tokens expiram em 30 dias.

[NOTE]
====

Essa configuração é usada por todos os tokens kubeconfig, exceto aqueles criados pela CLI para <<_disable_tokens_in_generated_kubeconfigs,gerar tokens kubeconfig>>.
====


[#_disable_tokens_in_generated_kubeconfigs]
== Desativar tokens em Kubeconfigs Gerados

Defina a configuração `kubeconfig-generate-token` para `false`. Essa configuração instrui o Rancher a não gerar mais automaticamente um token quando um usuário clica em baixar um arquivo kubeconfig. Quando essa configuração está desativada, um kubeconfig gerado referencia o {xref-kubectl}#_authentication_with_kubectl_and_kubeconfig_tokens_with_ttl[Rancher CLI] para recuperar um token de curta duração para o cluster. Quando esse kubeconfig é usado em um cliente, como `kubectl`, o Rancher CLI precisa ser instalado para completar a solicitação de login.

[#_token_hashing]
== Hashing de token

Você pode {xref-experimental-features}[habilitar o hashing de token], onde os tokens passam por um hash unidirecional usando o algoritmo SHA256. Este é um processo não reversível: uma vez habilitado, esse recurso não pode ser desativado. Você deve primeiro avaliar essa configuração em um ambiente de teste e/ou fazer backups antes de habilitar.

Este recurso afeta todos os tokens que incluem, mas não se limitam a, os seguintes:

* Tokens de kubeconfig
* Tokens Bearer, chaves/chamadas da API
* Tokens usados por operações internas

[#_token_settings]
== Configurações de token

Essas configurações globais afetam o comportamento do token do Rancher.

|===
| Configuração | Descrição

| <<_auth_user_session_ttl_minutes,`auth-user-session-ttl-minutes`>>
| TTL em minutos para um token de sessão de autenticação de usuário.

| <<_auth_user_session_idle_ttl_minutes, `auth-user-session-idle-ttl-minutes`>>
| TTL em minutos para um token de sessão de autenticação de usuário, sem atividade do usuário.

| <<_kubeconfig_default_token_ttl_minutes,`kubeconfig-default-token-ttl-minutes`>>
| TTL padrão aplicado a todos os tokens de kubeconfig, exceto para tokens <<_disable_tokens_in_generated_kubeconfigs,gerados pelo Rancher CLI>>.

| <<_auth_token_max_ttl_minutes,`auth-token-max-ttl-minutes`>>
| TTL máximo para todos os tokens, exceto aqueles controlados por <<_auth_user_session_ttl_minutes,`auth-user-session-ttl-minutes`>>.

| <<_kubeconfig_generate_token,`kubeconfig-generate-token`>>
| Se verdadeiro, gera automaticamente tokens quando um usuário baixa um kubeconfig.
|===

[#_auth_user_session_ttl_minutes]
=== auth-user-session-ttl-minutes

Duração do tempo de vida (TTL) em minutos, usada para determinar quando um token de sessão de autenticação de usuário expira. Quando expirar, o usuário deve fazer login e obter um novo token. Esta configuração não é afetada por <<_auth_token_max_ttl_minutes,`auth-token-max-ttl-minutes`>>. Tokens de sessão são criados quando um usuário faz login no Rancher.

[#_auth_user_session_idle_ttl_minutes]
=== auth-user-session-idle-ttl-minutes

Duração do tempo de vida (TTL) sem atividade do usuário para tokens de sessão de login, em minutos.
Por padrão, `auth-user-session-idle-ttl-minutes` é definido com o mesmo valor que <<_auth_user_session_ttl_minutes, `auth-user-session-ttl-minutes`>> (para compatibilidade retroativa). Nunca deve exceder o valor de `auth-user-session-ttl-minutes`.

[#_kubeconfig_default_token_ttl_minutes]
=== kubeconfig-default-token-ttl-minutes

Duração do tempo de vida (TTL) em minutos, usada para determinar quando um token kubeconfig expira. Quando o token expira, a API rejeita o token. Esta configuração não pode ser maior que <<_auth_token_max_ttl_minutes,`auth-token-max-ttl-minutes`>>. Esta configuração se aplica a tokens gerados em um arquivo kubeconfig solicitado, exceto para tokens <<_disable_tokens_in_generated_kubeconfigs,gerados pelo Rancher CLI>>. A partir do Rancher v2.8, a duração padrão é `43200`, o que significa que os tokens expiram em 30 dias.

[#_auth_token_max_ttl_minutes]
=== auth-token-max-ttl-minutes

Tempo máximo de vida (TTL) em minutos permitido para tokens de autenticação. Se um usuário tentar criar um token com um TTL maior que `auth-token-max-ttl-minutes`, o Rancher define o TTL do token para o valor de `auth-token-max-ttl-minutes`. Aplica-se a todos os tokens kubeconfig e tokens de API. A partir do Rancher v2.8, a duração padrão é `129600`, o que significa que os tokens expiram em 90 dias.

[#_kubeconfig_generate_token]
=== kubeconfig-generate-token

Quando verdadeiro, os kubeconfigs solicitados através da interface contêm um token válido. Quando falso, os kubeconfigs contêm um comando que usa o Rancher CLI para solicitar que o usuário faça login. {xref-kubectl}#_authentication_with_kubectl_and_kubeconfig_tokens_with_ttl[O CLI então recupera e armazena em cache um token para o usuário].
cli/kubectl.adoc#_authentication_with_kubectl_and_kubeconfig_tokens_with_ttl[O CLI então recupera e armazena em cache um token para o usuário].
