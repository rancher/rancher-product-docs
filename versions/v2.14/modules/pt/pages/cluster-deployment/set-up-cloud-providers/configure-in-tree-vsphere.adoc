= Configuração de um provedor de nuvem VMware vSphere in-tree
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2025-08-05
:page-revdate: {revdate}
:community-path: procedimentos/novos-guias-de-usuário/configuração-de-clusters-kubernetes-no-rancher/configurar-provedores-de-nuvem/configurar-vsphere-in-tree.adoc 
:product-path: cluster-deployment/set-up-cloud-providers/configure-in-tree-vsphere.adoc

Para configurar o provedor de nuvem VMware vSphere in-tree, siga estas etapas ao criar o cluster vSphere no Rancher:

. Defina a opção *Provedor de Nuvem* como `Custom` ou `Custom (In-Tree)`.
. Clique em *Editar como YAML*
. Insira a seguinte estrutura no YAML do cluster pré-populado. Essa estrutura deve ser colocada sob `rancher_kubernetes_engine_config`. Observe que o `name` _deve_ ser definido como `vsphere`.
+
[,yaml]
----
 rancher_kubernetes_engine_config:
   cloud_provider:
       name: vsphere
       vsphereCloudProvider:
           [Insert provider configuration]
----

O Rancher utiliza o RKE (Rancher Kubernetes Engine) para provisionar clusters Kubernetes. Consulte a https://rancher.com/docs/rke/latest/en/config-options/cloud-providers/vsphere/config-reference/[referência de configuração do vSphere na documentação do RKE] para detalhes sobre as propriedades da diretiva `vsphereCloudProvider`.
