= Configurez les composants d'Istio pour la gestion du trafic
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2025-10-01
:page-revdate: {revdate}
:community-path: how-to-guides/advanced-user-guides/istio-setup-guide/set-up-traffic-management.adoc 
:product-path: observability/istio/guides/set-up-traffic-management.adoc
ifeval::["{build-type}" != "community"]
:xref-istio-generate-and-view-traffic: xref:observability/istio/guides/generate-and-view-traffic.adoc
endif::[]
ifeval::["{build-type}" == "community"]
:xref-istio-generate-and-view-traffic: xref:how-to-guides/advanced-user-guides/istio-setup-guide/generate-and-view-traffic.adoc
endif::[]

[WARNING]
====
https://github.com/rancher/charts/tree/release-v2.11/charts/rancher-istio[Rancher-Istio] a cessé la prise en charge depuis Rancher v2.12.0{nbsp}; tournez-vous vers l'https://apps.rancher.io[SUSE Application Collection] d'Istio pour une sécurité renforcée (incluse dans les abonnements SUSE Rancher Prime).
Des informations détaillées peuvent être trouvées dans https://forums.suse.com/t/deprecation-of-rancher-istio/45043[cette annonce].
====

Un avantage central de la gestion du trafic dans Istio est qu'elle permet le routage dynamique des requêtes. Parmi les applications courantes du routage dynamique des requêtes, on trouve les déploiements canari et les déploiements bleu/vert. Les deux ressources clés dans la gestion du trafic d'Istio sont _les services virtuels_ et _les règles de destination_.

* https://istio.io/docs/reference/config/networking/v1alpha3/virtual-service/[Les services virtuels] interceptent et dirigent le trafic vers vos services Kubernetes, vous permettant de diviser les pourcentages de trafic d'une requête entre différents services. Vous pouvez les utiliser pour définir un ensemble de règles de routage à appliquer lorsqu'un hôte est adressé.
* https://istio.io/docs/reference/config/networking/v1alpha3/destination-rule/[Les règles de destination] servent de source unique de vérité sur les versions de service disponibles pour recevoir du trafic des services virtuels. Vous pouvez utiliser ces ressources pour définir des politiques qui s'appliquent au trafic destiné à un service après que le routage a eu lieu.

Cette section décrit comment ajouter un exemple de service virtuel qui correspond au `reviews` microservice dans l'appli d'exemple BookInfo. Le but de ce service est de diviser le trafic entre deux versions du service `reviews`.

Dans cet exemple, nous prenons le trafic vers le `reviews` service et l'interceptons afin que 50 % de celui-ci aille à `v1` du service et 50 % aille à `v2`.

Après le déploiement de ce service virtuel, nous allons générer du trafic et voir grâce à la visualisation Kiali que le trafic est réparti également entre les deux versions du service.

Pour déployer le service virtuel et les règles de destination pour le `reviews` service,

. Cliquez sur *☰ > Gestion des clusters*.
. Allez dans le cluster où Istio est installé et cliquez sur *Explorer*.
. Dans le cluster où Istio est installé, cliquez sur menu:Istio[Règles de destination] dans la barre de navigation à gauche.
. Cliquez sur *Create*.
. Copiez et collez le yaml de la règle de destination fourni ci-dessous.
. Cliquez sur *Create*.
. Cliquez sur *Modifier en YAML* et utilisez cette configuration{nbsp}:
+
[,yaml]
----
 apiVersion: networking.istio.io/v1alpha3
 kind: DestinationRule
 metadata:
   name: reviews
 spec:
   host: reviews
   subsets:
   - name: v1
     labels:
       version: v1
   - name: v2
     labels:
       version: v2
   - name: v3
     labels:
       version: v3
----

. Cliquez sur *Create*.

Ensuite, pour déployer le service virtuel qui fournit le routage du trafic utilisant la règle de destination{nbsp}:

. Cliquez sur *VirtualService* dans la barre de navigation latérale.
. Cliquez sur *Créer à partir de Yaml*.
. Copiez et collez le yaml de VirtualService fourni ci-dessous.
. Cliquez sur *Create*.

[,yaml]
----
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: reviews
spec:
  hosts:
  - reviews
  http:
  - route:
    - destination:
        host: reviews
        subset: v1
      weight: 50
    - destination:
        host: reviews
        subset: v3
      weight: 50
---
----

*Résultat :* Lorsque vous générez du trafic vers ce service (par exemple, en actualisant l'URL de la passerelle d'entrée), le graphique de trafic Kiali reflétera que le trafic vers le `reviews` service est réparti également entre `v1` et `v3`.

[#__xref_istio_generate_and_view_traffic_next_generate_and_view_traffic]
== {xref-istio-generate-and-view-traffic}[Suivant{nbsp}: Générer et afficher le trafic]
