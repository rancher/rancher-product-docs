= La configuration du fournisseur de cloud Google Compute Engine
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2025-08-05
:page-revdate: {revdate}
:community-path: how-to-guides/new-user-guides/kubernetes-clusters-in-rancher-setup/set-up-cloud-providers/google-compute-engine.adoc 
:product-path: cluster-deployment/set-up-cloud-providers/google-compute-engine.adoc

Dans cette section, vous apprendrez à activer le fournisseur de cloud Google Compute Engine (GCE) pour des clusters personnalisés dans Rancher. Un cluster personnalisé est celui dans lequel Rancher installe Kubernetes sur des nœuds existants.

La documentation officielle de Kubernetes pour le fournisseur de cloud GCE est https://github.com/kubernetes/website/blob/release-1.18/content/en/docs/concepts/cluster-administration/cloud-providers.md#gce[ici.]

[NOTE]
.Conditions préalables :
====

Le compte de service disposant d'un accès `Identity and API` sur GCE nécessite la permission `Computer Admin`.
====


Si vous utilisez Calico,

. Cliquez sur *☰ > Gestion des clusters*.
. Sur la page *Clusters*, allez au cluster personnalisé et cliquez sur _*⋮ > Modifier YAML._ Entrez la configuration suivante{nbsp}:
+
[,yaml]
----
 rancher_kubernetes_engine_config:
   cloud_provider:
     name: gce
     customCloudProvider: |-
       [Global]
       project-id=<your project ID, optional>
       network-name=<your network, optional if using default network>
       subnetwork-name=<your subnetwork of the above network, optional if using default network>
       node-instance-prefix=<your instance group name/your instance name specific prefix, required>
       node-tags=<your network tags, must patch one or some tags, required>
   network:
     options:
       calico_cloud_provider: "gce"
     plugin: "calico"
----

Si vous utilisez Canal ou Flannel,

. Cliquez sur *☰ > Gestion des clusters*.
. Sur la page *Clusters*, allez au cluster personnalisé et cliquez sur _*⋮ > Modifier YAML._ Entrez la configuration suivante{nbsp}:
+
[,yaml]
----
 rancher_kubernetes_engine_config:
   cloud_provider:
     name: gce
     customCloudProvider: |-
       [Global]
       project-id=<your project ID, optional>
       network-name=<your network, optional if using default network>
       subnetwork-name=<your subnetwork of the above network, optional if using default network>
       node-instance-prefix=<your instance group name/your instance name specific prefix, required>
       node-tags=<your network tags, must patch one or some tags, required>
   services:
     kube_controller:
       extra_args:
         configure-cloud-routes: true # we need to allow the cloud provider configure the routes for the hosts
----
