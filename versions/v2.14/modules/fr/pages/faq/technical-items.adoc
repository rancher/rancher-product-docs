= FAQ technique
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2026-02-18
:page-revdate: {revdate}
:community-path: faq/technical-items.adoc 
:product-path: faq/technical-items.adoc
ifeval::["{build-type}" != "community"]
:xref-rke2-config: xref:cluster-deployment/configuration/rke2.adoc
:xref-set-up-cloud-providers: xref:cluster-deployment/set-up-cloud-providers/set-up-cloud-providers.adoc
:xref-clean-cluster-nodes: xref:cluster-admin/manage-clusters/clean-cluster-nodes.adoc
:xref-k3s-config: xref:cluster-deployment/configuration/k3s.adoc
:xref-logging-troubleshooting: xref:observability/logging/troubleshooting.adoc
endif::[]
ifeval::["{build-type}" == "community"]
:xref-rke2-config: xref:guides-de-référence/configuration-de-cluster/configuration-du-serveur-rancher/configuration-du-cluster-rke2.adoc
:xref-set-up-cloud-providers: xref:guides-procédures/guides-nouveaux-utilisateurs/kubernetes-clusters-dans-rancher-configuration/configuration-cloud-providers/index.adoc
:xref-clean-cluster-nodes: xref:how-to-guides/new-user-guides/manage-clusters/clean-cluster-nodes.adoc
:xref-k3s-config: xref:guides-de-référence/configuration-de-cluster/configuration-du-serveur-rancher/configuration-du-cluster-k3s.adoc
:xref-logging-troubleshooting: xref:troubleshooting/other-troubleshooting-tips/logging.adoc
endif::[]

[#_how_can_i_reset_the_administrator_password]
== Comment puis-je réinitialiser le mot de passe administrateur{nbsp}?

Installation de Docker{nbsp}:

 $ docker exec -ti <container_id> reset-password
 New password for default administrator (user-xxxxx):
 <new_password>

Installation de Kubernetes (Helm){nbsp}:

 $ KUBECONFIG=./kube_config_cluster.yml
 $ kubectl --kubeconfig $KUBECONFIG -n cattle-system exec $(kubectl --kubeconfig $KUBECONFIG -n cattle-system get pods -l app=rancher --no-headers | head -1 | awk '{ print $1 }') -c rancher -- reset-password
 New password for default administrator (user-xxxxx):
 <new_password>

ifeval::["{build-type}" == "product"]
Installation de Kubernetes (Helm - *Applicable à Rancher Prime v2.13.1*){nbsp}:

 $ KUBECONFIG=./kube_config_cluster.yml
 $ kubectl --kubeconfig $KUBECONFIG -n cattle-system exec $(kubectl --kubeconfig $KUBECONFIG -n cattle-system get pods -l app=rancher-prime --no-headers | head -1 | awk '{ print $1 }') -c rancher-prime -- reset-password
 New password for default administrator (user-xxxxx):
 <new_password>
endif::[]

[#_i_deleted_deactivated_the_last_admin_how_can_i_fix_it]
== J'ai supprimé/désactivé le dernier administrateur, comment puis-je y remédier{nbsp}?

Installation de Docker{nbsp}:

 $ docker exec -ti <container_id> ensure-default-admin
 New default administrator (user-xxxxx)
 New password for default administrator (user-xxxxx):
 <new_password>

Installation de Kubernetes (Helm){nbsp}:

 $ KUBECONFIG=./kube_config_cluster.yml
 $ kubectl --kubeconfig $KUBECONFIG -n cattle-system exec $(kubectl --kubeconfig $KUBECONFIG -n cattle-system get pods -l app=rancher | grep '1/1' | head -1 | awk '{ print $1 }') -- ensure-default-admin
 New password for default administrator (user-xxxxx):
 <new_password>

ifeval::["{build-type}" == "product"]
Installation de Kubernetes (Helm - *Applicable à Rancher Prime v2.13.1*){nbsp}:

 $ KUBECONFIG=./kube_config_cluster.yml
 $ kubectl --kubeconfig $KUBECONFIG -n cattle-system exec $(kubectl --kubeconfig $KUBECONFIG -n cattle-system get pods -l app=rancher-prime | grep '1/1' | head -1 | awk '{ print $1 }') -- ensure-default-admin
 New password for default administrator (user-xxxxx):
 <new_password>
endif::[]

[#_how_can_i_enable_debug_logging]
== Comment puis-je activer la journalisation de débogage{nbsp}?

Voir {xref-logging-troubleshooting}[Dépannage{nbsp}: Journalisation]

[#_my_clusterip_does_not_respond_to_ping]
== Mon ClusterIP ne répond pas au ping

ClusterIP est une IP virtuelle, qui ne répondra pas au ping. Le meilleur moyen de tester si le ClusterIP est configuré correctement est d'utiliser `curl` pour accéder à l'IP et au port afin de voir s'il répond.

[#_where_can_i_manage_node_templates]
== Où puis-je gérer les modèles de nœuds{nbsp}?

Les modèles de nœuds sont accessibles en ouvrant votre menu de compte (en haut à droite) et en sélectionnant `Node Templates`.

[#_why_is_my_layer_4_load_balancer_in_pending_state]
== Pourquoi mon équilibreur de charge de couche 4 est-il dans l'état `Pending`{nbsp}?

L'équilibreur de charge de couche 4 est créé en tant que `type: LoadBalancer`. Dans Kubernetes, cela nécessite un fournisseur de cloud ou un contrôleur capable de satisfaire ces demandes, sinon ils resteront dans l'état `Pending` indéfiniment. Plus d'informations peuvent être trouvées sur {xref-set-up-cloud-providers}[Fournisseurs de cloud] ou https://kubernetes.io/docs/tasks/access-application-cluster/create-external-load-balancer/[Créer un équilibreur de charge externe]

[#_where_is_the_state_of_rancher_stored]
== Où l'état de Rancher est-il stocké{nbsp}?

* Installation de Docker{nbsp}: dans l'etcd intégré du conteneur `rancher/rancher`, situé à `/var/lib/rancher`.
* Installation de Kubernetes{nbsp}: l'emplacement par défaut se trouve dans les répertoires `/var/lib/rancher/rke2` ou `/var/lib/rancher/k3s` du cluster RKE2/K3s respectif créé pour exécuter Rancher.

[#_how_are_the_supported_docker_versions_determined]
== Comment les versions Docker prises en charge sont-elles déterminées{nbsp}?

Nous suivons les versions Docker validées pour les versions Kubernetes en amont. Les versions validées peuvent être trouvées sous https://github.com/kubernetes/kubernetes/blob/master/CHANGELOG/CHANGELOG-1.10.md#external-dependencies[Dépendances externes] dans le CHANGELOG.md de la version Kubernetes.

[#_how_can_i_access_nodes_created_by_rancher]
== Comment puis-je accéder aux nœuds créés par Rancher{nbsp}?

Les clés SSH pour accéder aux nœuds créés par Rancher peuvent être téléchargées via la vue *Nœuds*. Choisissez le nœud auquel vous souhaitez accéder et cliquez sur le bouton vertical ⋮ à la fin de la ligne, puis choisissez *Télécharger les clés* comme indiqué dans l'image ci-dessous.

image::downloadsshkeys.png[Télécharger les clés]

Décompressez le fichier zip téléchargé et utilisez le fichier `id_rsa` pour vous connecter à votre hôte. Assurez-vous d'utiliser le bon nom d'utilisateur (`rancher` ou `docker` pour RancherOS, `ubuntu` pour Ubuntu, `ec2-user` pour Amazon Linux)

 $ ssh -i id_rsa user@ip_of_node

[#_how_can_i_automate_task_x_in_rancher]
== Comment puis-je automatiser la tâche X dans Rancher{nbsp}?

L'interface utilisateur se compose de fichiers statiques et fonctionne en fonction des réponses de l'API. Cela signifie que chaque action/tâche que vous pouvez exécuter dans l'interface utilisateur peut être automatisée via l'API. Il y a 2 façons de faire cela{nbsp}:

* Visitez `https://your_rancher_ip/v3` et parcourez les options de l'API.
* Capturez les appels API lors de l'utilisation de l'interface utilisateur (le plus couramment utilisé pour cela est https://developers.google.com/web/tools/chrome-devtools/#network[Outils de développement Chrome], mais vous pouvez utiliser ce que vous voulez)

[#_the_ip_address_of_a_node_changed_how_can_i_recover]
== L'adresse IP d'un nœud a changé, comment puis-je récupérer{nbsp}?

Un nœud doit avoir une adresse IP statique configurée (ou une adresse IP réservée via DHCP). Si l'IP d'un nœud a changé, vous devrez le retirer du cluster et le réajouter. Après sa suppression, Rancher mettra à jour le cluster pour le ramener à l'état correct. Si le cluster n'est plus dans l'état `Provisioning`, le nœud est retiré du cluster.

Lorsque l'adresse IP du nœud a changé, Rancher a perdu la connexion avec le nœud, il ne pourra donc pas nettoyer le nœud correctement. Voir {xref-clean-cluster-nodes}[Nettoyage des nœuds du cluster] pour nettoyer le nœud.

Lorsque le nœud est retiré du cluster et que le nœud est nettoyé, vous pouvez réajouter le nœud au cluster.

[#_how_can_i_add_more_arguments_binds_environment_variables_to_kubernetes_components_in_a_rancher_launched_kubernetes_cluster]
== Comment puis-je ajouter plus d'arguments/liaisons/variables d'environnement aux composants Kubernetes dans un cluster Kubernetes lancé par Rancher{nbsp}?

Vous pouvez ajouter plus d'arguments/liaisons/variables d'environnement via le {xref-rke2-config}#_configuration_du_cluster[Fichier de configuration RKE2] ou {xref-k3s-config}#_configuration_du_cluster[Fichier de configuration K3s].

[#_how_do_i_check_if_my_certificate_chain_is_valid]
== Comment vérifier si ma chaîne de certificats est valide{nbsp}?

Utilisez la commande `openssl verify` pour valider votre chaîne de certificats{nbsp}:

[TIP]
====

Configurez `SSL_CERT_DIR` et `SSL_CERT_FILE` à un emplacement fictif pour vous assurer que les certificats installés par le système d'exploitation ne sont pas utilisés lors de la vérification manuelle.
====


----
SSL_CERT_DIR=/dummy SSL_CERT_FILE=/dummy openssl verify -CAfile ca.pem rancher.yourdomain.com.pem
rancher.yourdomain.com.pem: OK
----

Si vous recevez l'erreur `unable to get local issuer certificate`, la chaîne est incomplète. Cela signifie généralement qu'il y a un certificat CA intermédiaire qui a émis votre certificat de serveur. Si vous avez déjà ce certificat, vous pouvez l'utiliser dans la vérification du certificat comme indiqué ci-dessous{nbsp}:

----
SSL_CERT_DIR=/dummy SSL_CERT_FILE=/dummy openssl verify -CAfile ca.pem -untrusted intermediate.pem rancher.yourdomain.com.pem
rancher.yourdomain.com.pem: OK
----

Si vous avez vérifié avec succès votre chaîne de certificats, vous devez inclure les certificats CA intermédiaires nécessaires dans le certificat de serveur pour compléter la chaîne de certificats pour toute connexion établie avec Rancher (par exemple, par l'agent Rancher). L'ordre des certificats dans le fichier de certificat de serveur doit d'abord être le certificat de serveur lui-même (contenu de `rancher.yourdomain.com.pem`), suivi des certificats CA intermédiaires (contenu de `intermediate.pem`).

----
-----BEGIN CERTIFICATE-----
%YOUR_CERTIFICATE%
-----END CERTIFICATE-----
-----BEGIN CERTIFICATE-----
%YOUR_INTERMEDIATE_CERTIFICATE%
-----END CERTIFICATE-----
----

Si vous obtenez toujours des erreurs lors de la vérification, vous pouvez récupérer le sujet et l'émetteur du certificat de serveur en utilisant la commande suivante{nbsp}:

----
openssl x509 -noout -subject -issuer -in rancher.yourdomain.com.pem
subject= /C=GB/ST=England/O=Alice Ltd/CN=rancher.yourdomain.com
issuer= /C=GB/ST=England/O=Alice Ltd/CN=Alice Intermediate CA
----

[#_how_do_i_check_common_name_and_subject_alternative_names_in_my_server_certificate]
== Comment vérifier `Common Name` et `Subject Alternative Names` dans mon certificat de serveur{nbsp}?

Bien qu'une entrée dans `Subject Alternative Names` soit techniquement requise, avoir le nom d'hôte à la fois dans `Common Name` et comme entrée dans `Subject Alternative Names` vous donne une compatibilité maximale avec les anciens navigateurs/applications.

Vérifiez `Common Name`{nbsp}:

----
openssl x509 -noout -subject -in cert.pem
subject= /CN=rancher.my.org
----

Vérifiez `Subject Alternative Names`{nbsp}:

----
openssl x509 -noout -in cert.pem -text | grep DNS
                DNS:rancher.my.org
----

[#_why_does_it_take_5_minutes_for_a_pod_to_be_rescheduled_when_a_node_has_failed]
== Pourquoi faut-il plus de 5 minutes pour qu'un pod soit reprogrammé lorsqu'un nœud a échoué{nbsp}?

Cela est dû à une combinaison des paramètres par défaut de Kubernetes suivants{nbsp}:

* kubelet
 ** `node-status-update-frequency` : Spécifie la fréquence à laquelle kubelet publie l'état des nœuds au maître (par défaut 10s)
* kube-controller-manager
 ** `node-monitor-period` : La période de synchronisation de NodeStatus dans NodeController (par défaut 5s)
 ** `node-monitor-grace-period` : Durée pendant laquelle nous permettons à un nœud en cours d'exécution d'être non réactif avant de le marquer comme non sain (par défaut 40s)
 ** `pod-eviction-timeout` : La période de grâce pour supprimer les pods sur les nœuds échoués (par défaut 5m0s)

Voir https://kubernetes.io/docs/reference/command-line-tools-reference/kubelet/[Kubernetes: kubelet] et https://kubernetes.io/docs/reference/command-line-tools-reference/kube-controller-manager/[Kubernetes: kube-controller-manager] pour plus d'informations sur ces paramètres.

Dans Kubernetes v1.13, la fonctionnalité `TaintBasedEvictions` est activée par défaut. Voir https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/#taint-based-evictions[Kubernetes: évictions basées sur les Taints] pour plus d'informations.

* kube-apiserver (Kubernetes v1.13 et supérieur)
 ** `default-not-ready-toleration-seconds` : Indique les tolerationSeconds de la tolérance pour notReady:NoExecute qui est ajoutée par défaut à chaque pod qui n'a pas déjà une telle tolérance.
 ** `default-unreachable-toleration-seconds` : Indique les tolerationSeconds de la tolérance pour unreachable:NoExecute qui est ajoutée par défaut à chaque pod qui n'a pas déjà une telle tolérance.

[#_can_i_use_keyboard_shortcuts_in_the_ui]
== Puis-je utiliser des raccourcis clavier dans l'interface utilisateur{nbsp}?

Oui, la plupart des parties de l'interface utilisateur peuvent être atteintes en utilisant des raccourcis clavier. Pour un aperçu des raccourcis disponibles, appuyez sur `?` n'importe où dans l'interface utilisateur.
