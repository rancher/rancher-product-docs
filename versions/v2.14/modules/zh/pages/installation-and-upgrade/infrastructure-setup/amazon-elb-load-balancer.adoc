= 设置 Amazon ELB 网络负载均衡器
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2026-03-20
:page-revdate: {revdate}
:community-path: how-to-guides/new-user-guides/infrastructure-setup/amazon-elb-load-balancer.adoc 
:product-path: installation-and-upgrade/infrastructure-setup/amazon-elb-load-balancer.adoc

本 how-to 指南描述了如何在 Amazon 的 EC2 服务中设置网络负载平衡器 (NLB)，该负载平衡器将流量引导到 EC2 上的多个实例。

这些示例显示了负载均衡器被配置为将流量引导到三个 Rancher 服务器节点。如果 Rancher 安装在 K3s Kubernetes 集群上，则只需要两个节点。

本教程介绍了一种可能的设置负载平衡器的方法，而不是唯一的方法。其他类型的负载平衡器，例如经典负载平衡器或应用负载平衡器，也可以将流量引导到 Rancher 服务器节点。

Rancher 仅支持在以 `tcp` 模式终止 443 端口流量时使用 Amazon NLB，而不支持在 `tls` 模式下终止流量。这是因为 NLB 在终止于 NLB 时不会在请求中注入正确的头信息。这意味着如果您想使用 Amazon 证书管理器 (ACM) 管理的证书，您应该使用 ALB。

[#_requirements]
== 要求

这些说明假设您已经在 EC2 中创建了 Linux 实例。负载均衡器将流量引导到这些节点。

[#_1_create_target_groups]
== 1.创建目标组

首先为 *TCP* 协议创建两个目标组，一个使用 TCP 端口 443，另一个使用 TCP 端口 80（提供重定向到 TCP 端口 443）。您将把 Linux 节点添加到这些组中。

您第一个 NLB 配置步骤是创建两个目标组。从技术上讲，仅需要端口 443 来访问 Rancher，但添加一个端口 80 的侦听器是方便的，因为流量将自动重定向到端口 443。

Traefik Ingress 应该将流量从端口 80 重定向到端口 443。

. 登录到 https://console.aws.amazon.com/ec2/[Amazon AWS 控制台] 开始。确保选择您的 EC2 实例（Linux 节点）创建的 *区域*。
. 选择 *服务* 并选择 *EC2*，找到 *负载均衡* 部分并打开 *目标组*。
. 点击 *创建目标组* 创建第一个目标组，针对 TCP 端口 443。

[NOTE]
====
有关 Traefik Ingress 健康检查的详细信息，请参阅 <<_health_check_paths_for_traefik_ingresses,本节。>>
====

[discrete]
[#_target_group_tcp_port_443]
==== 目标组 (TCP 端口 443)

根据下表配置第一个目标组。

|===
| 选项 | 设置

| 目标组名称
| `rancher-tcp-443`

| 目标类型
| `instance`

| 协议
| `TCP`

| 端口
| `443`

| VPC
| 选择您的 VPC
|===

健康检查设置：

|===
| 选项 | 设置

| 协议
| TCP

| 端口
| `override`,`80`

| 健康阈值
| `3`

| 不健康阈值
| `3`

| 超时
| `6 seconds`

| 间隔
| `10 seconds`
|===

点击 *创建目标组* 创建第二个目标组，针对 TCP 端口 80。

[discrete]
[#_target_group_tcp_port_80]
==== 目标组 (TCP 端口 80)

根据下表配置第二个目标组。

|===
| 选项 | 设置

| 目标组名称
| `rancher-tcp-80`

| 目标类型
| `instance`

| 协议
| `TCP`

| 端口
| `80`

| VPC
| 选择您的 VPC
|===

健康检查设置：

|===
| 选项 | 设置

| 协议
| TCP

| 端口
| `traffic port`

| 健康阈值
| `3`

| 不健康阈值
| `3`

| 超时
| `6 seconds`

| 间隔
| `10 seconds`
|===

[#_2_register_targets]
== 2.注册目标

接下来，将您的 Linux 节点添加到两个目标组。

选择名为 *rancher-tcp-443* 的目标组，点击 *目标* 标签并选择 *编辑*。

image::ha/nlb/edit-targetgroup-443.png[]

选择您想要添加的实例（Linux 节点），然后点击 *添加到已注册*。

'''

.截图 添加目标到目标组 TCP 端口 443
image::ha/nlb/add-targets-targetgroup-443.png[]

'''

.截图 已添加目标到目标组 TCP 端口 443
image::ha/nlb/added-targets-targetgroup-443.png[]

当实例被添加后，点击屏幕右下角的 *保存*。

重复这些步骤，将 *rancher-tcp-443* 替换为 *rancher-tcp-80*。相同的实例需要作为目标添加到此目标组。

[#_3_create_your_nlb]
== 3.创建您的 NLB

使用亚马逊的向导创建网络负载均衡器。在此过程中，您将添加在 <<_1_create_target_groups, 中创建的目标组。创建目标组>>。

. 在您的网页浏览器中，导航到 https://console.aws.amazon.com/ec2/[亚马逊 EC2 控制台]。
. 在导航窗格中，选择 *负载均衡* > *负载均衡器*。
. 单击 *创建负载均衡器*。
. 选择 *网络负载均衡器* 并单击 *创建*。然后完成每个表单。

* <<_step_1_configure_load_balancer,第 1 步：配置负载均衡器>>
* <<_step_2_configure_routing,第 2 步：配置路由>>
* <<_step_3_register_targets,第 3 步：注册目标>>
* <<_step_4_review,第 4 步：回顾>>

[#_step_1_configure_load_balancer]
=== 第 1 步：配置负载均衡器

在表单中设置以下字段：

* *名称:* `rancher`
* *方案:* `internal` 或 `internet-facing`.您为 NLB 选择的方案取决于您的实例和 VPC 的配置。如果您的实例没有关联公共 IP，或者您只会在内部访问 Rancher，您应该将 NLB 方案设置为 `internal` 而不是 `internet-facing`。
* *侦听器:*负载均衡器协议应为 `TCP`，相应的负载均衡器端口应设置为 `443`。
* *可用性区域:*选择您的 *VPC* 和 *可用性区域*。

[#_step_2_configure_routing]
=== 第 2 步：配置路由

. 从 *目标组* 下拉菜单中选择 *现有目标组*。
. 从 *名称* 下拉菜单中选择 `rancher-tcp-443`。
. 打开 *高级健康检查设置*，并将 *间隔* 配置为 `10 seconds`。

[#_step_3_register_targets]
=== 第三步：注册目标

由于您之前已注册目标，您只需点击 *下一步：回顾*。

[#_step_4_review]
=== 第四步：回顾

查看负载均衡器的详细信息，当您满意时点击 *创建*。

在 AWS 创建 NLB 后，点击 *关闭*。

[#_4_add_listener_to_nlb_for_tcp_port_80]
== 4.为 TCP 端口 80 向 NLB 添加侦听器

. 选择您新创建的 NLB，并选择 *侦听器* 选项卡。
. 点击 *添加侦听器*。
. 使用 `TCP`:``80`` 作为 *协议* :*端口*
. 点击 *添加操作* 并选择 *转发到..*。
. 从 *转发到* 下拉菜单中选择 `rancher-tcp-80`。
. 点击屏幕右上角的 *保存*。

[#_health_check_paths_for_traefik_ingresses]
== Traefik Ingress 的健康检查路径列表

K3s Kubernetes 集群使用 Traefik 作为默认的 Ingress。

健康检查路径是 `/ping`。默认情况下，`/ping` 始终匹配（无论 Host），并且始终返回来自 https://docs.traefik.io/operations/ping/[Traefik 本身] 的响应。

为了模拟准确的健康检查，最佳实践是尽可能使用 Host 头（Rancher 主机名）结合 `/ping` 或 `/healthz`，以获取来自 Rancher Pods 的响应，而不是 Ingress。
