= 备份 {rancher-product-name}
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2025-08-13
:page-revdate: {revdate}
:community-path: how-to-guides/new-user-guides/backup-restore-and-disaster-recovery/back-up-rancher.adoc 
:product-path: rancher-admin/back-up-restore-and-disaster-recovery/back-up.adoc
:experimental:
ifeval::["{build-type}" != "community"]
:xref-fleet-overview: xref:integrations/fleet/overview.adoc
:xref-rancher-backup-config: xref:rancher-admin/back-up-restore-and-disaster-recovery/configuration/backup.adoc
:xref-rancher-backup-storage-config: xref:rancher-admin/back-up-restore-and-disaster-recovery/configuration/storage.adoc
:xref-migrate-cluster: xref:rancher-admin/back-up-restore-and-disaster-recovery/migrate-to-a-new-cluster.adoc
:xref-back-restore-example-configs: xref:rancher-admin/back-up-restore-and-disaster-recovery/configuration/examples.adoc
endif::[]
ifeval::["{build-type}" == "community"]
:xref-fleet-overview: xref:integrations-in-rancher/fleet/overview.adoc
:xref-rancher-backup-config: xref:reference-guides/backup-restore-configuration/backup-configuration.adoc
:xref-rancher-backup-storage-config: xref:reference-guides/backup-restore-configuration/storage-configuration.adoc
:xref-migrate-cluster: xref:how-to-guides/new-user-guides/backup-restore-and-disaster-recovery/migrate-rancher-to-new-cluster.adoc
:xref-back-restore-example-configs: xref:reference-guides/backup-restore-configuration/examples.adoc
endif::[]

在本节中，您将学习如何备份运行在任何 Kubernetes 集群上的 Rancher。要备份使用 Docker 安装的 Rancher，请参考 xref:[单节点备份] 的说明。

备份-恢复操作符需要在本地群集中安装，并且仅备份 Rancher APP。备份和恢复操作仅在本地 Kubernetes 集群中执行。

请注意，rancher-backup 操作符版本 2.x.x 适用于 Rancher v2.6.x。

[CAUTION]
====

在将备份恢复到新的 Rancher 设置时，新设置的版本应与备份创建时的版本相同。在恢复备份时，还应考虑 Kubernetes 版本，因为集群和备份文件中支持的 apiVersion 可能不同。
====


[#_prerequisites]
== 先决条件

Rancher 版本必须为 v2.5.0 及以上。

有关在 Rancher v2.6.3 中将现有备份文件恢复到 v1.22 集群的帮助，请参阅 {xref-migrate-cluster}#_2_restore_from_backup_using_a_restore_custom_resource[这里]。

[#_1_install_the_rancher_backup_operator]
== 1.安装 Rancher Backup 操作符

备份存储位置是操作符级别的设置，因此在安装或升级 Rancher Backups 应用程序时需要进行配置。

备份以 .tar.gz 文件格式创建。这些文件可以推送到 S3 或 Minio，或者可以存储在持久卷中。

. 在左上角，点击 *☰ > 集群管理*。
. 在 *集群* 页面上，转到 `local` 集群并单击 *探索*。`local` 集群运行 Rancher 服务器。
. 点击 menu:应用[图表]。
. 单击 *Rancher 备份*。
. 单击 *安装*。
. 配置默认存储位置。如需帮助，请参阅 {xref-rancher-backup-storage-config}[存储配置部分]。
. 单击 *安装*。

[NOTE]
====

在使用 backup-restore-operator 执行恢复后，Fleet 中存在一个已知问题：用于 clientSecretName 和 helmSecretName 的机密未包含在 Fleet gitrepos 中。请参考 {xref-fleet-overview}#_故障排除[此处]以获取解决方法。
====


[#_2_perform_a_backup]
== 2.执行备份

要执行备份，必须创建一个类型为 Backup 的自定义资源。

. 在左上角，点击 *☰ > 集群管理*。
. 在 *集群* 页面上，转到 `local` 集群并单击 *探索*。
. 在左侧导航栏中，点击 menu:Rancher 备份[备份]。
. 单击*创建*。
. 使用表单或 YAML 编辑器创建备份。
. 要使用表单配置备份详细信息，请点击 *创建* 并参考 {xref-rancher-backup-config}[配置参考] 和 {xref-back-restore-example-configs}#_backup[示例]。
. 要使用 YAML 编辑器，我们可以点击 menu:创建[从 YAML 创建]。输入备份 YAML。此示例备份自定义资源将创建加密的定期备份到 S3。该 APP 使用 `credentialSecretNamespace` 的值来确定查找 S3 备份密钥的位置：
+
[,yaml]
----
 apiVersion: resources.cattle.io/v1
 kind: Backup
 metadata:
   name: s3-recurring-backup
 spec:
   storageLocation:
     s3:
       credentialSecretName: s3-creds
       credentialSecretNamespace: default
       bucketName: rancher-backups
       folder: rancher
       region: us-west-2
       endpoint: s3.us-west-2.amazonaws.com
   resourceSetName: rancher-resource-set-full
   encryptionConfigSecretName: encryptionconfig
   schedule: "@every 1h"
   retentionCount: 10
----
+

[NOTE]
====
在使用 YAML 编辑器创建备份资源时，`resourceSetName` 必须设置为 `rancher-resource-set-full` 或 `rancher-resource-set-basic`。
====

+
有关配置备份的帮助，请参阅 {xref-rancher-backup-config}[配置参考] 和 {xref-back-restore-example-configs}#_backup[示例]。
+

[CAUTION]
====
`rancher-backup` 操作符不会保存 EncryptionConfiguration 文件。创建加密备份时，必须保存 EncryptionConfiguration 文件的内容，并且在从此备份恢复时必须使用相同的文件。
====


. 单击*创建*。

*结果：*备份文件是在备份自定义资源中配置的存储位置创建的。此文件的名称在执行恢复时使用。
