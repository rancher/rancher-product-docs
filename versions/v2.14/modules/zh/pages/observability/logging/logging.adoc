= {rancher-product-name}与日志服务的集成
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2025-08-05
:page-revdate: {revdate}
:community-path: integrations-in-rancher/logging/index.adoc 
:product-path: observability/logging/logging.adoc
:description: Rancher与流行的日志服务集成。了解与日志服务集成的要求和好处，并在您的集群上启用日志记录。
ifeval::["{build-type}" != "community"]
:xref-logging-taints-and-tolerations: xref:observability/logging/taints-and-tolerations.adoc
:xref-logging-helm-chart-options: xref:observability/logging/logging-helm-chart-options.adoc
:xref-logging-architecture: xref:observability/logging/logging-architecture.adoc
:xref-logging-rbac: xref:observability/logging/rbac-for-logging.adoc
:xref-logging-flows-clusterflows: xref:observability/logging/custom-resource-configuration/flows-and-clusterflows.adoc
:xref-logging-outputs-clusteroutputs: xref:observability/logging/custom-resource-configuration/outputs-and-clusteroutputs.adoc
endif::[]
ifeval::["{build-type}" == "community"]
:xref-logging-taints-and-tolerations: xref:integrations-in-rancher/logging/taints-and-tolerations.adoc
:xref-logging-helm-chart-options: xref:integrations-in-rancher/logging/logging-helm-chart-options.adoc
:xref-logging-architecture: xref:integrations-in-rancher/logging/logging-architecture.adoc
:xref-logging-rbac: xref:integrations-in-rancher/logging/rbac-for-logging.adoc
:xref-logging-flows-clusterflows: xref:integrations-in-rancher/logging/custom-resource-configuration/flows-and-clusterflows.adoc
:xref-logging-outputs-clusteroutputs: xref:integrations-in-rancher/logging/custom-resource-configuration/outputs-and-clusteroutputs.adoc
endif::[]

https://kube-logging.github.io/docs/[日志记录操作员]现在取代了以前的内部解决方案，成为Rancher的日志解决方案。

[#_enabling_logging]
== 启用日志记录

您可以通过访问应用页面并安装日志应用来为Rancher管理的集群启用日志记录。

. 转到您想要安装日志记录的集群，然后单击*应用*。
. 单击*日志记录*应用。
. 滚动到Helm图表README的底部，然后单击*安装*。

*结果：*日志应用已部署在`cattle-logging-system`名称空间中。

[#_uninstall_logging]
== 卸载日志记录

. 转到您想要安装日志记录的集群，然后单击*应用*。
. 单击*已安装的应用程序*。
. 转到`cattle-logging-system`名称空间并勾选`rancher-logging`和`rancher-logging-crd`的框。
. 单击*删除*。
. 确认*删除*。

*结果* `rancher-logging`已卸载。

[#_architecture]
== 体系结构

有关日志应用如何工作的更多信息，请参见{xref-logging-architecture}[本节。]

[#_role_based_access_control]
== 基于角色的访问控制

Rancher日志有两个角色，`logging-admin`和`logging-view`。有关如何以及何时使用这些角色的更多信息，请参见{xref-logging-rbac}[此页面。]

[#_configuring_logging_custom_resources]
== 配置日志自定义资源

要管理`Flows,` `ClusterFlows`、`Outputs`和`ClusterOutputs`，

. 在左上角，单击*☰ > 集群管理*。
. 在*集群*页面，转到您要配置日志记录自定义资源的集群，并单击*浏览*。
. 在左侧导航栏中，单击*日志记录*。

[#_flows_and_clusterflows]
=== 流和集群流

有关配置`Flows`和`ClusterFlows`的帮助，请参见{xref-logging-flows-clusterflows}[此页面。]

[#_outputs_and_clusteroutputs]
=== 输出和集群输出

有关配置`Outputs`和`ClusterOutputs`的帮助，请参见{xref-logging-outputs-clusteroutputs}[此页面。]

[#_using_a_custom_hosttailer_image]
== 使用自定义 HostTailer 镜像

要为`HostTailer`资源使用自定义镜像，您需要在`HostTailer`资源中每个`fileTailer`的`containerOverrides`部分中指定该镜像。

[,yaml]
----
apiVersion: logging-extensions.banzaicloud.io/v1alpha1
kind: HostTailer
metadata:
  name: cluster-system-log
spec:
  workloadMetaOverrides:
    annotations: {}
    labels: {}
  fileTailers:
    - disabled: false
      name: kubelet-log
      path: /var/lib/rancher/rke2/agent/logs/*.log
      containerOverrides:
        image: <your_registry>/<your_image>:<your_tag>
    - disabled: false
      name: containerd-log
      path: /var/lib/rancher/rke2/agent/containerd/*.log
      containerOverrides:
        image: <your_registry>/<your_image>:<your_tag>
    - name: kube-audit
      path: /var/log/kube-audit/audit-log.json
      disabled: false
      containerOverrides:
        image: <your_registry>/<your_image>:<your_tag>
----

[#_configuring_the_logging_helm_chart]
== 配置日志记录 Helm 图表

有关安装或升级日志应用程序时可以配置的选项列表，请参见{xref-logging-helm-chart-options}[此页面。]

[#_windows_support]
=== Windows 支持

您可以从 Windows 节点xref:./logging-helm-chart-options.adoc#_enabledisable_windows_node_logging[启用日志记录]。

[#_working_with_a_custom_docker_root_directory]
=== 使用自定义 Docker 根目录

有关使用自定义 Docker 根目录的详细信息，请参见xref:./logging-helm-chart-options.adoc#_working_with_a_custom_docker_root_directory[本节。]

[#_working_with_taints_and_tolerations]
=== 处理污点和容忍

有关如何与日志应用程序一起使用污点和容忍的信息，请参见{xref-logging-taints-and-tolerations}[此页面。]

[#_logging_v2_with_selinux]
=== 带有 SELinux 的日志记录 V2

有关在启用了 SELinux 的节点上启用日志应用程序的信息，请参见xref:./logging-helm-chart-options.adoc#_enabling_the_logging_application_to_work_with_selinux[本节。]

[#_additional_logging_sources]
=== 附加日志记录源

默认情况下，Rancher 为所有集群类型的控制平面组件和节点组件收集日志。在某些情况下，可以收集额外的日志。有关详细信息，请参见xref:./logging-helm-chart-options.adoc#_additional_logging_sources[本节]。

[#_troubleshooting]
== 查错

[#_the_logging_buffer_overloads_pods]
=== 日志缓冲区超载了 Pod。

根据您的配置，默认缓冲区大小可能过大，导致 Pod 故障。减少负载的一种方法是降低记录器的刷新间隔。这可以防止日志过度填充缓冲区。您还可以添加更多的刷新线程，以处理许多日志同时尝试填充缓冲区的时刻。

有关如何配置日志缓冲区以满足您组织需求的更完整描述，请参见官方的日志操作员文档，内容包括 https://kube-logging.github.io/docs/configuration/plugins/outputs/buffer/[缓冲区]和 https://kube-logging.github.io/docs/logging-infrastructure/fluentd/[Fluentd 配置]。

[#_the_cattle_logging_namespace_being_recreated]
=== 正在重新创建 `cattle-logging` 名称空间。

如果您的集群之前在旧版 Rancher UI 的全局视图中部署了日志，您可能会遇到其 `cattle-logging` 名称空间不断被重新创建的问题。

解决方案是从管理集群的特定的名称空间中删除所有 `clusterloggings.management.cattle.io` 和 `projectloggings.management.cattle.io` 自定义资源。
这些自定义资源的存在会导致 Rancher 在下游集群中创建 `cattle-logging` 名称空间（如果它不存在的话）。

集群名称空间与集群 ID 匹配，因此我们需要找到每个集群的集群 ID。

. 在左上角，单击*☰ > 集群管理*。
. 在 *集群* 页面上，转到您想要获取 ID 的集群，然后单击 *浏览*。
. 从以下 URL 中复制 `<cluster-id>` 部分。`<cluster-id>` 部分是集群的名称空间名称。

[,bash]
----
# Cluster Management UI
https://<your-url>/c/<cluster-id>/

# Cluster Dashboard
https://<your-url>/dashboard/c/<cluster-id>/
----

现在我们有了 `<cluster-id>` 名称空间，可以删除导致 `cattle-logging` 不断被重新创建的 CR。
_警告:_ 请确保日志（从旧版 Rancher UI 的全局视图中安装的版本）当前未在使用中。

[,bash]
----
kubectl delete crd clusterloggings.management.cattle.io -n <cluster-id>
kubectl delete crd projectloggings.management.cattle.io -n <cluster-id>
----
