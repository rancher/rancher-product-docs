= Fehlerbehebung bei Worker-Knoten und allgemeinen Komponenten
ifeval::["{build-type}" != "srfa"]
:page-languages: [en, zh]
endif::[]
:revdate: 2025-08-05
:page-revdate: {revdate}
:community-path: troubleshooting/kubernetes-components/troubleshooting-worker-nodes-and-generic-components.adoc 
:product-path: troubleshooting/kubernetes-components/troubleshooting-worker-nodes-and-generic-components.adoc

Dieser Abschnitt gilt für jeden Knoten, da er Komponenten enthält, die auf Knoten mit jeder Rolle ausgeführt werden.

[#_check_if_the_containers_are_running]
== Überprüfen Sie, ob die Container ausgeführt werden

Es gibt zwei spezifische Container, die auf Knoten mit der Rolle `worker` gestartet werden:

* kubelet
* kube-proxy

Die Container sollten den Status `Up` haben. Die Dauer, die nach `Up` angezeigt wird, ist die Zeit, die der Container bereits läuft.

----
docker ps -a -f=name='kubelet|kube-proxy'
----

Beispielausgabe:

----
CONTAINER ID        IMAGE                                COMMAND                  CREATED             STATUS              PORTS               NAMES
158d0dcc33a5        rancher/hyperkube:v1.11.5-rancher1   "/opt/rke-tools/en..."   3 hours ago         Up 3 hours                              kube-proxy
a30717ecfb55        rancher/hyperkube:v1.11.5-rancher1   "/opt/rke-tools/en..."   3 hours ago         Up 3 hours                              kubelet
----

[#_container_logging]
== Container-Protokollierung

Die Protokollierung der Container kann Informationen darüber enthalten, was das Problem sein könnte.

----
docker logs kubelet
docker logs kube-proxy
----
